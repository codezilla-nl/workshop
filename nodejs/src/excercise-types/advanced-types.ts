console.log('========= Excercise 2 =========');

// 1. Create a function which returns a Promise with a string and call it

// 2. Create an interface with some readonly keys and use that as a return interface in your function and call it

// 3. Create an interface with some optional keys and use that as a return interface in your function and call it

// 4. Create a function with optional parameters and call it

